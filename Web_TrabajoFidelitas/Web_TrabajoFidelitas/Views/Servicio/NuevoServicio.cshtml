@model Web_TrabajoFidelitas.Entidades.Servicios

@{
    ViewBag.Title = "Registrar Servicio";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<a class="btn btn-primary m-2 my-4" href="@Url.Action("MostrarServicios","Servicio")"><i class="bi bi-arrow-left-circle"></i> Regresar </a>
<div class="card shadow mb-4">
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">
            <div class="card-body" style="margin-top: 20px;">
                <div class="table-responsive" style="overflow-x: hidden;">
                    <div id="dataTable_wrapper" class="dataTables_wrapper dt-bootstrap4">
                        <h3>Agregar Servicio</h3>
                        <hr />
                        @if (ViewBag.MsjPantalla != null)
                        {
                            <div class="alert alert-warning" role="alert" style="text-align:center">
                                @ViewBag.MsjPantalla
                            </div>
                        }

                        @using (Html.BeginForm("NuevoServicio", "Servicio", FormMethod.Post, new { @class = "row g-3 needs-validation", novalidate = "novalidate" }))
                        {
                            <div class="form-group my-4">
                                @Html.Label("Nombre del servicio a realizar")
                                @Html.TextBoxFor(m => m.NombreServicio, new { @class = "form-control", @placeholder = "Nombre", @required = "true", @type = "text", @maxlength = "100" })
                                @Html.ValidationMessageFor(m => m.NombreServicio, "", new { @class = "text-danger" })
                                <div class="invalid-feedback">Por favor, ingrese el nombre del servicio</div>
                            </div>
                            <div class="form-group my-4">
                                @Html.Label("Precio del servicio")
                                @Html.TextBoxFor(m => m.PrecioServicio, new { @class = "form-control", @placeholder = "Teléfono", @required = "true", @type = "tel", })
                                @Html.ValidationMessageFor(m => m.PrecioServicio, "", new { @class = "text-danger" })
                                <div class="invalid-feedback">Por favor, ingrese el Precio del Servicio</div>
                            </div>
                            <div class="form-group my-4">
                                @Html.Label("Descripcion del Servicio")
                                @Html.TextBoxFor(m => m.DescripcionServicio, new { @class = "form-control", @placeholder = "Dirección", @required = "true", @type = "text", @maxlength = "255" })
                                @Html.ValidationMessageFor(m => m.DescripcionServicio, "", new { @class = "text-danger" })
                                <div class="invalid-feedback">Por favor, ingrese la descripcion del servicio </div>
                            </div>
                            

                            <div style="display: flex; align-items: center; flex-direction: column;">
                                <input type="submit" class="btn btn-primary btn-block" value="Agregar servicio" style="margin-top: 20px; margin-bottom: 20px;">
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
